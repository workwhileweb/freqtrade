<!-- Auto-translated from docs/ by script. Please review technical terms. -->

# Thay đổi nổi bật

- ...

## Cách cập nhật

Như mọi khi, bạn có thể cập nhật bot của mình bằng một trong các lệnh sau:

### docker-compose```bash
docker-compose pull
docker-compose up -d
```### Cài đặt thông qua tập lệnh thiết lập``` bash
# Deactivate venv and run 
./setup.sh --update
```### Cài đặt gốc đơn giản``` bash
git pull
pip install -U -r requirements.txt
```<chi tiết>
<summary>Mở rộng toàn bộ nhật ký thay đổi</summary>```
<Paste your changelog here>
```</ chi tiết>